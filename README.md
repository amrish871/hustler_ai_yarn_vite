# hustlerWeb (Yarn v1 + Vite + TypeScript + Tailwind + Turborepo)

Quick start:

```bash
# ensure yarn v1 is installed globally
npm install -g yarn@1

yarn install
yarn dev:web   # starts web-app on 3000
yarn dev:admin # starts admin-app on 3001
yarn build
```
